import React, { ReactNode } from "react";
import Cards from "./Cards";
import { Box } from "@mui/material";

const Home = () => {
  return (
    <Box>
      <Cards />
    </Box>
  );
};

export default Home;
